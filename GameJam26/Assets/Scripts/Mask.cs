using System.Linq;
using UnityEngine;

public class Mask : MonoBehaviour
{
    [SerializeField] Transform[] MountPointsBaseHead;
    [SerializeField] Transform[] MountPointsEyes;
    [SerializeField] Transform[] MountPointsNose;
    [SerializeField] Transform[] MountPointsHat;
    [SerializeField] Transform[] MountPointsColor;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
//        Instantiate(myPrefab).transform;
//        MountPointsBaseHead.Append()
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
